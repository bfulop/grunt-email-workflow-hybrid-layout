<tr>
    <td>
        <table class="outer" align="center" cellpadding="0" cellspacing="0">
            <tr>
                <td class="full-width-image {{ class }}">
                    <!--[if (gte mso 9)|(IE)]>
                    <a href="{{ link }}"><img src="{{ src-desktop }}" width="{{ parameters.pagewidth }}" alt="{{ alt }}"/></a>
                    <![endif]-->
                    <a href="{{ link }}" class="hide-mso"><img alt="{{ alt }}" width="{{ parameters.pagewidth }}" src="{{ src-mobile }}" class="hide-mso" /></a>
                </td>
            </tr>
        </table>
    </td>
</tr>